module beast.backend.interpreter.op.basic;

import beast.backend.interpreter.op.toolkit;
pragma( inline ):

void op_allocLocal( MemoryPtr op1, MemoryPtr op2, MemoryPtr op3 ) {
	assert( 0 ); // This instruction should be handled directly in the interpreter
}