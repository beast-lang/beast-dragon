// Module names have to match with source file name (and project directory structure)
module t_1_helloworld;

// Void is a type, types start with a capital
Void main() {
	// We don't have string implemented yet, so this is our equivalent to "Hello, world!"
	print( 1 ); //! stdout: 1
}